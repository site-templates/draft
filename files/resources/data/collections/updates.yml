# Column types for updates.json — the source of truth the builder
# reads: richtext columns open in the rich editor (Content drawer
# and any section repeater listing this collection).
fields:
    title: { type: text }
    description: { type: textarea }
    category: { type: text }
    date: { type: text }
    readTime: { type: text }
    author: { type: text }
    authorImage: { type: image }
    link: { type: url }
    image: { type: image }
    imageAlt: { type: text }
    slug: { type: text }
    authorRole: { type: text }
    content: { type: richtext }
